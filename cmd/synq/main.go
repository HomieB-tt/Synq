// Command synq is the entry point for the Synq terminal client.
package main

import (
	"fmt"
	"os"
)

func main() {
	// TODO: initialize crypto key store, db cache, ws client, and the
	// Bubble Tea root model (internal/app), then run the program.
	fmt.Fprintln(os.Stderr, "synq: not yet implemented")
	os.Exit(1)
}
